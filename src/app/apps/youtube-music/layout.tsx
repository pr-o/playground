import type { ReactNode } from 'react';

export default function YoutubeMusicCloneLayout({ children }: { children: ReactNode }) {
  return <div className="music-theme min-h-screen">{children}</div>;
}
